# Save/open layer restoration — 2026-09-05

1. [done] Inspect dirty worktree, SSOT, saved-file contents and session log; preserve all pre-existing edits and original survey files.
2. [done] Cause: saved project registry has 8–10 layers, but its layer tree has only 2–3 basemaps; recovery ignores valid layers with missing tree nodes.
3. [done] Additional confirmed risk: nested open/autosave can persist a partial project; repeated layer-tree model replacement logs invalid proxy indexes.
4. [done] RED: registry-only missing nodes, hidden forced visible, unresolved nodes deleted, model replacement, failed commit overwrites workspace, Save As changes survey_area_2 to survey_area. Temporary fixtures only.
5. [in progress] LayerOps repairs missing tree entries; MainWindow retains model and guards reads/saves. Finish commit failure propagation and Save As actual table paths + WAL-safe copy in SurveyStorage.
6. [done] Field XML: project EPSG:5187; survey layers 5187, imported cultural-site layers 5179. OTF stays enabled; WMS GetMap/scale recipe unchanged because failure is tree membership.
7. [pending] Independent review; Release build, relevant tests/full ctest, app save/open check, smoke-quit; publish only when no user app will be force-closed.
8. [pending] Done check: saving and reopening the same/copy survey restores geometry, attributes, layer list, names, styles and visibility; report remaining limitations, no commit.

MCP constraint: Sequential Thinking only (called through installed local MCP SDK because the current session has no refreshed tool registration).
Graph: qgis_expert + arcgis_expert + design_field_check, then ka-developer implementation, reviewer, tester using Codex native workers; Grok-specific host is unavailable.

## Baseline evidence

- Desktop 최종.gpkg: 10 registered layers, only 2 legend nodes. Desktop 안동시_복사본_복사본.gpkg: 8 registered layers, 3 legend nodes.
- Original hashes: 최종 F7BEC0DBE0007CD8A7D35525F5BC924F3352224F6765FB2FE3A3073A844B4B64; 안동시_복사본_복사본 06E830FA22DAB55B970257B1DA2D3951B1003C7E496A94B3A1C2735229F49C91.
- Build/portable baseline identical; desktop shortcut is 고고학 전용 GIS.lnk → A:/hgis/dist/ka-hgis-portable/start.bat.
- Window RED logs: build/qa/save-open-red.txt and build/qa/save-open-commit-red.txt. Byte hash alone is insufficient for a live GPKG (SQLite WAL); assert embedded project content too.
- Local cookbook physical pp.16/24 and installed qgslayertreelayer.h: tree nodes can exist before layers resolve. Missing layer pointer during read is not a deleted node.

## Resume — 2026-09-05

1. [done] Re-read NOW, SSOT, current diff and RED logs; current workspace is A:/hgis (older handoff paths are stale).
2. [done] Step 5 code is present: commitSurveyEdits returns failure, Save As keeps source table options, copySurvey snapshots committed WAL with VACUUM INTO.
3. [done] Independent review and fresh Release build (exit 0). Initial targeted CTest: 1/3 passed; invalid GPKG replaces current work, Save As test uses wrong field, and source-string test requires obsolete local names. Native CTest exit not captured because PowerShell stderr handling aborted the wrapper (exit 1); do not infer its native code.
4. [in progress] Correct the two test defects; add regression coverage for stale capture pointers on project switch and same-key/different-table recovery. Fix only these save/open blockers plus invalid-file preflight; then repeat relevant tests and full CTest. Field fixture has 10 registered layers/2 legend nodes, all local vectors point to ./최종.gpkg, and its WAL is empty; use it only through temporary copies.
5. [pending] Run smoke-quit with isolated user settings, then publish only when no user ka-hgis process is running; compare build/portable SHA-256.
6. [pending] Record actual exit codes and evidence, update NOW, retain explicit desktop click validation as a field follow-up; no commit.

Available MCP now includes Sequential Thinking (called natively this turn). Context7 and Grok-specific host remain unavailable; use the existing plan's Codex worker fallback and local QGIS references.
