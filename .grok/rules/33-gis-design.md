# GIS designers (always on every development)

User standing order: **모든 개발에 디자이너 에이전트 참가**. Slash: `/ka-design`.

Spawn this turn, parallel, read-only first:

| Agent | Hat |
| --- | --- |
| `ka-color` | ink / paper / satellite contrast |
| `ka-symbol` | fill / line / marker / 축척자 style |
| `ka-ui` | QSS, toolbar, sheet chrome mm |
| `ka-ux` | field workflow, empty states |

If types are missing, spawn four `designer` agents with `[ka-color]` … in `description`.

Skip only a one-character identifier typo with no user-visible pixels.
Do not ship sheet/map visuals from C++ only. Do not clone ArcMap chrome.
