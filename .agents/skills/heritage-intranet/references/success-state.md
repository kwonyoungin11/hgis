# 성공 기준과 장애 기록

2026-09-13 기준. 이후 상태는 저장소 `.codex/NOW.md`의 최신 항목 및 사용자의 실제 확인으로 갱신한다. 새 성공 확인 없이 오래된 성공을 확대 해석하지 않는다.

## 최신 확정 기준 — PID 15408

사용자가 2026-09-13 PID 15408 실행 뒤 **“지금 파일이 가장깔끔하게 빠르게 받아지는 파일이다”**라고 확인했다. 이 확인을 현재 성공·복원 기준으로 삼는다. 속도 평가는 사용자의 실사용 관찰이며, 에이전트가 이번 실행의 소요 시간을 측정하거나 모든 시군을 재검사한 것은 아니다.

- 확정 복구본: `D:/hgis/build/recovery/heritage-user-confirmed-pid15408-20260913/`.
- 실제 실행 파일: `build/Release/ka-hgis.exe` (위 복구본 기준 상대 경로).
- 실행 파일 SHA256: `DECB7FE6A7393667AE51475AA2C4EE5E4F3628E63015F014A8E995EC794B0175`.
- `manifest.json`: 실행 파일 해시, 사용자 확인 문구, 소스·빌드 관련 183개 파일의 경로와 SHA256. 소스는 `src/`, 빌드 설정·실행 스크립트는 원래 상대 경로에 있다. OSGeo4W/Qt 런타임 전체 배포본은 아니다.
- 빌드 기록: `build/qa/heritage-pid34708-source-restore-build.txt`. PID 34708 관련 소스를 복원하고 재컴파일한 **새 실행 PID가 15408**이다. 34708 원본 exe와 동일하다는 의미가 아니다.
- 원래 보관본도 유지한다: `build/recovery/before-pid34708-source-restore-20260913-165548/restored-pid15408/`. 상위 폴더의 변경 전 exe를 성공 exe로 잘못 선택하지 않는다.

현재 계약은 공식 전체다운로드 1회 → 실제 DownloadCompleted → 저장 파일 존재·크기 확인 → ZIP/SHP 검사·동기 지도 적재 성공 → 다음 종류다. 실패는 성공·0건으로 바꾸지 않는다. 번호, 자동 열, 그룹 라벨 후속 변경과 16:35 바이트 대조/오류 상세 전달은 제거된 상태다. 표시 기능 요청으로 다운로드 경로까지 수정하지 않는다.

복원 요청 시 현재 작업을 먼저 백업하고 이 명세로 대상 파일을 확인한다. **관련 파일만** 복원하며 소스 복원은 수정 시간을 갱신해 영향받은 구현·호출자·MOC가 함께 재컴파일되게 한다. 실행 파일 자체의 복원을 요청받은 경우 위 보존 exe와 해시를 사용한다. 성공 확정본은 덮어쓰지 않는다.

실사용 확인은 사용자가 한다. 새 지시 없이 자동 다운로드·다른 브라우저 비교·사용자 조사 조작을 하지 않는다. 코드 변경 후 필요한 빌드와 정상 실행 스크립트로 앱 열기까지만 수행한다. 계정·브라우저 프로필·다운로드 SHP는 복구본이나 스킬에 포함하지 않는다.

## 과거 기준점 — 최신 PID 15408보다 우선하지 않음

- **제주시 13:42:53~13:45:56**: 사용자가 여섯 자료 모두 다운로드·적재됐다고 확인했다. 요청 후 수신 시작은 각 종류 2~9초였다.
- 소스 백업: `D:/hgis/build/recovery/heritage-user-confirmed-all-download-20260913/`.
- 이 백업에는 Browser cpp/h, Flow cpp, Import cpp 네 파일만 있다. 프로젝트 전체 스냅샷으로 취급하지 않는다.
- 이후 번호·범례 기능을 추가했다가 제거했다. 사용자는 제거한 버전에서 다시 빨리 받아졌다고 확인했다. 지연의 인과관계 전체가 확정된 것은 아니다.
- 그룹 이름 일괄 메뉴도 별도로 추가했다가 사용자 요청으로 제거했다. 새 요청 없이 번호/자동 열/그룹 메뉴를 자동 복구하지 않는다.

과거 네 파일 SHA256 (PID 15408에서도 같은 내용이지만, 이것만으로 전체 빌드를 식별하지 않는다):

| 파일 | SHA256 |
|---|---|
| src/app/KaHeritageBrowser.cpp | 8115E50B058BC5F10B69B441B8D4D531747A7C088B08BF72349A45CB4FF498C0 |
| src/app/KaHeritageBrowser.h | C75E2CF6FA5420195A47A90FD81D3B63BD943F08DEDBCA2FCC48A7F79CF34752 |
| src/core/HeritageIntranetFlow.cpp | 3E542DB0ADE669C14A6ECC689A69832AAABA6E1D2687E7374F52EDCBBADDB734 |
| src/core/HeritageImport.cpp | 5D49A16BE2C509957021972D2B0D6FA4C5B48AC9EB323C5C0C368FA42157506C |

## 다시 헷갈리지 말 것

- 09:09 안동시 지정유산 1종 성공은 초기 사례다. 최신 여섯 종류 성공보다 우선하지 않는다. 초기 인계문서의 “유일한 성공 조합”은 이후 사실과 다르다.
- 공식 `downloadFiles(...all...)`는 사이트가 자체 URL을 만들고 창을 연다. 실제 버튼 클릭이 현재 성공 경로다. `onclick` 속성만 찾거나 같은 이름의 지도 검색 함수를 직접 호출하면 안 된다.
- 지정/현상변경은 `codedetaCd/codeCdSg`, 다른 자료는 `bjdCd/bjdCd1` 등 폼이 다르다. 지도 패널의 동명 필드와 혼동하지 않는다. 현재 Flow의 문서/폼 선택을 보존한다.
- 전 단계의 함수 호출 성공은 다음 화면 도착이나 파일 완료가 아니다. 실제 폼/검색 교체/다운로드 완료/적재 결과를 구분한다.
- 수신 전 131초가 걸린 실행이 있었지만 같은 코드로 빠른 수신도 있었다. 외부 응답 지연을 무조건 앱 고정 대기나 파일 용량 탓으로 돌리지 않는다.
- 14:55, 14:57 안동시의 완료 신호 후 ZIP은 실제 0바이트였다. 이 실패 판정은 유지한다. 빈 응답 원인은 확정하지 못했다.

## 14:59 복원 앱 종료의 빌드 함정

`KaHeritageBrowser.cpp/h`를 Copy-Item으로 복원하면서 수정 시간이 13:41로 돌아갔다. `KaHeritageBrowser.obj`는 멤버가 추가된 14:18 버전 그대로인데 `MainWindow.obj`는 복원 헤더로 14:51에 컴파일됐다. 빌드 성공에도 버전이 섞였으며 이후 힙 손상 종료가 발생했다.

복원 파일 시간을 갱신해 Browser, MainWindow, MOC가 모두 재컴파일된 것을 확인했다. 기록은 `build/qa/heritage-restore-recompile-build.txt`다. 이 때문에 복원 여부는 **내용 해시와 재컴파일 로그를 함께** 확인해야 한다.

덤프는 `%LOCALAPPDATA%/CrashDumps/`에 있으며 민감한 메모리를 포함할 수 있다. 로컬에서 필요한 스택만 확인하고 스킬/저장소/외부 서비스에 복사하지 않는다. WER의 근접 심볼명만으로 Qt 내부 실제 함수를 단정하지 않는다.

## 15:54 강릉시 불완전 ZIP

- 지정유산_260913155429243.zip: 34,275바이트, 로컬 헤더 4개, 중앙 디렉터리/종료 레코드 없음. Python zipfile에서 정상 ZIP이 아님을 확인했다. 이전 성공 파일 3개는 각각 90,844바이트/24개 항목의 정상 ZIP이다.
- 파일 검증 실패가 지도 적재/범례 이전에 발생했다. 불완전 응답이 발생한 원인은 아직 확정하지 않았다. 실패를 성공으로 처리하거나 오래된 캐시를 이번 다운로드로 대체하지 않는다.
- 당시 MainWindow에 실제 압축/적재 오류 전달을 추가했으나 현재 PID 15408에서는 제거됐다. 아래 항목은 당시 이력이며 재적용 지시가 아니다.

## 16:02 사용자 요청 재복원

범례 자동 열/도면 갱신 보류/오류 상세 전달을 제거해 사용자 성공 확인 상태로 복원했다. 번호/그룹 라벨 메뉴도 제거 상태다. 다운로드 네 파일의 기준 해시는 유지된다. 직전 백업은 build/recovery/restore-success-20260913-160216/. 새 요청 없이 제거한 기능을 재추가하지 않는다.

## 16:06 복원 후 재실패와 백업의 한계

사용자가 소스 복원 빌드에서도 0바이트 ZIP 실패를 확인했다. 네 파일 기준 해시는 일치하지만 실제 다운로드 성공은 회복되지 않았다. 확인한 로컬 백업에는 여섯 종류 성공 당시 exe/전체 프로젝트 스냅샷이 없다. 12:05 exe를 13:42 성공 빌드로 잘못 대체하지 않는다. 소스 복원만으로 '성공 상태 완전 복원'이라고 보고하지 않는다.

## 16:35 실제 수신량 확인

성공 당시 여섯 ZIP의 CRC와 13 SHP를 재확인했다. 임시 프로필의 강릉시 1회 공식 다운로드에서는 예상/수신/저장이 모두 71,289바이트, DownloadCompleted였으나 ZIP 자체가 손상됐다. 기존 프로필/지도 적재 순서만을 원인으로 추정하지 않는다. 당시 예상/수신/저장 바이트 대조와 실제 압축 오류 전달을 추가했으나 PID 15408 복원에서 제거했다. 원래 성공 백업은 변경하지 않았다.

## 2026-09-13 16:56 사용자 지정 PID 34708 관련 소스 복원

- 사용자가 PID 34708 기준 복원을 요청했다. 당시 exe 자체가 없어 동일 바이너리 복원은 아니며, 남아 있는 관련 소스 백업으로 재빌드했다.
- KaHeritageBrowser.cpp는 heritage-user-confirmed-all-download-20260913, MainWindow.cpp는 before-heritage-layout-deferral-20260913, MainWindow.h는 before-heritage-import-error-detail-20260913 백업으로 복원했다. 제거한 차이는 16:35 수신/저장량 대조와 오류 상세 전달이다. 기존 DownloadCompleted/파일 존재·크기/ZIP·SHP 검사/동기 지도 적재 후 다음 종류 계약은 유지한다.
- Browser.h/Flow/Import/DrawingStudio/HeritageSiteLegend/LayoutService는 번호·범례 작업 전 관련 백업과 일치하여 다시 덮어쓰지 않았다. 기타 미커밋 작업, 계정, 원본 조사, 내려받은 자료는 보존했다.
- 변경 전 소스 3개와 exe: build/recovery/before-pid34708-source-restore-20260913-165548/. manifest.json에 원본·복원 해시와 경로가 있다. 복원 파일은 현재 시각으로 기록했다.
- Release 빌드 통과: build/qa/heritage-pid34708-source-restore-build.txt. 정상 run-ka-hgis.ps1로 PID 15408을 실행했다. 그 뒤 사용자가 빠른 다운로드 성공을 확인했으며 맨 위 최신 확정 기준에 반영했다.

## 17:12 이후 재실패와 사용자 요청에 따른 자동 다시 받기

PID15408의 이전 성공 확인을 모든 새 지역의 성공으로 확대하지 않는다. 같은 구미에서 17:00 정상 ZIP 뒤 17:12에는 앞 16항목만 있는 불완전 ZIP이 왔고 경산 17:13은 0바이트였다. 다운로드 조건/인코딩은 구미 성공과 같았다. 서버 내부 원인까지 확정한 것은 아니다.

사용자가 “다운로드가 안 받아졌으면 다시 받게 하라, 에러를 내게 말고”라고 요청해 Browser cpp/h에 자동 복구를 추가했다. 같은 종류의 종료된 빈 응답·불완전 ZIP·일시적 전송 중단만 5/10/20/40/60초(이후 60초) 간격으로 다시 받는다. 요청별 공식 버튼 1회이며 중복 전송과 다음 종류 진행을 막는다. 정상 파일은 기존 동기 검사/적재 후 진행하고 중지할 수 있다. 다운로드 응답 캐시 방지 헤더만 추가하며 공식 URL/폼/세션은 바꾸지 않는다. 저장/권한 오류나 적재 실패를 무조건 재다운로드로 돌리지 않는다.

이번 Release 빌드는 통과했으나 실제 복구 성공은 사용자 확인 전이다. PID15408 원본 성공 백업은 보존한다. 최신 코드 상태는 .codex/NOW.md 맨 위를 따른다. 수치지형도 코드는 이번 변경 대상이 아니다.

당시 추가한 ZIP 사전 검사도 CP949로 처음부터 열고 스레드 로컬 설정을 원복했다. 기본 인코딩으로 한 번 열면 GDAL이 깨진 한국어 파일명을 캐시하여 뒤의 적재까지 영향을 준다. 합성 메모리 ZIP으로 확인했다. 아래 복원에서 이 사전 검사는 제거했으며, 원래 Import의 ZIP/SHP 검사는 유지한다.

## 2026-09-13 17:49 사용자 지정 PID 35732 소스 복구

사용자가 공식 전체다운로드 버튼으로 변경한 PID35732 안내문을 직접 지정하고 “복구하라”고 요청했다. NOW의 당시 실행 기록, `heritage-official-download-click-build.txt`, 직후 보존된 `heritage-user-confirmed-all-download-20260913/`의 다운로드 소스 네 파일을 확인했다. 해당 네 파일의 해시는 위 표 및 PID15408 대응 소스와 같다. 실제 PID35732 exe의 바이트 단위 복원이 아닌 소스 복구·재빌드다.

Browser cpp/h, Flow cpp, Import cpp를 이 백업으로 돌리고 수정 시간을 현재로 갱신한다. 후속 자동 재시도/헤더/ZIP 사전 검사와 작업 중이던 폼 새로 받기·0건 재검색을 제거했다. 폐기한 동작에만 해당하는 새 테스트도 제거했다. 기존 공식 버튼 1회 → 실제 DownloadCompleted → 파일 존재·크기 및 ZIP/SHP 확인 → 동기 지도 적재 → 다음 종류 계약은 그대로다. 기존 검색 0건 처리도 당시 코드대로다. 복구 전 소스와 exe는 `build/recovery/before-pid35732-restore-20260913-174958/`에 보존했다.

빌드와 실행 결과는 NOW 맨 위에 기록한다. 자동 실서버 다운로드를 하지 않으며 새 실행의 실제 수신 성공은 사용자가 확인한다. 이 요청을 새 재시도 기능 추가 요청으로 해석하지 않는다.
