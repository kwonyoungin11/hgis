# 2026-09-13 17:57 사용자 지정 복원 상태

사용자가 제주시 정상 ZIP 270,959바이트와 불완전 ZIP 161,726바이트를 비교한 오후 5:57 기록 시점으로 복원을 요청했다. 이 시점은 분석 문서만 추가했으며 제품 소스는 바뀌지 않았다. GitHub main에는 이 상태의 소스, 빌드 설정, 테스트, 문서와 문화재인트라넷 스킬을 보존한다.

## 소스 기준

| 파일 | SHA256 |
|---|---|
| src/app/KaHeritageBrowser.cpp | 8115E50B058BC5F10B69B441B8D4D531747A7C088B08BF72349A45CB4FF498C0 |
| src/app/KaHeritageBrowser.h | C75E2CF6FA5420195A47A90FD81D3B63BD943F08DEDBCA2FCC48A7F79CF34752 |
| src/core/HeritageIntranetFlow.cpp | 3E542DB0ADE669C14A6ECC689A69832AAABA6E1D2687E7374F52EDCBBADDB734 |
| src/core/HeritageImport.cpp | 5D49A16BE2C509957021972D2B0D6FA4C5B48AC9EB323C5C0C368FA42157506C |

Browser는 로컬 `build/recovery/before-heritage-long-wait-20260913-175939/`, Flow/Import는 `build/recovery/heritage-user-confirmed-all-download-20260913/`와 대조했다. 해당 백업 디렉터리는 로컬 전용이다. 다른 PC에서는 이 문서가 포함된 Git 커밋의 소스를 빌드한다.

공식 전체다운로드 버튼 1회 → 실제 DownloadCompleted → 저장 파일 확인 → ZIP/SHP 검사·동기 지도 적재 성공 → 다음 종류 순서를 유지한다. 이후의 30초 추가 대기, 5초 변경 제안, 번호·범례 후속 변경은 적용하지 않았다. 이 복원은 새 실사용 다운로드 성공을 뜻하지 않는다.

## 빌드·실행

기존 `scripts/dev-env.ps1` 환경에서 `cmake --build --preset release --target ka-hgis`로 Release 빌드했다. 복원 헤더와 구현의 수정 시간을 갱신하고 Browser, MainWindow, Qt MOC 재컴파일을 확인했다. 로컬 빌드 로그는 `build/qa/heritage-revert-long-wait-build.txt`다.

앱은 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run-ka-hgis.ps1`로 연다. 실제 다운로드 확인은 사용자가 수행한다.

다운로드 원본, 조사 데이터, 계정·설정 값, 빌드 산출물과 오래된 dist 포터블은 GitHub 등록 대상에서 제외한다. 앱의 기존 GPLv2+ 라이선스를 유지한다. 공개 테스트의 API 키 표본은 합성 값만 사용한다.
